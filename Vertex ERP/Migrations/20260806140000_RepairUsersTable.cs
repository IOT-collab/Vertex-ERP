using Microsoft.EntityFrameworkCore.Migrations;
using Microsoft.EntityFrameworkCore.Infrastructure;
using VertexERP.Data;

#nullable disable

namespace Vertex_ERP.Migrations
{
    /// <summary>
    /// Restores the login table when it was removed outside EF migrations.
    /// The SQL is intentionally idempotent, so it is safe for both existing
    /// databases and a newly created database.
    /// </summary>
    [DbContext(typeof(ApplicationDbContext))]
    [Migration("20260806140000_RepairUsersTable")]
    public partial class RepairUsersTable : Migration
    {
        protected override void Up(MigrationBuilder migrationBuilder)
        {
            migrationBuilder.Sql("""
                CREATE TABLE IF NOT EXISTS "Users" (
                    "Id" integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                    "Username" character varying(50) NOT NULL,
                    "NormalizedUsername" character varying(50) NOT NULL,
                    "PasswordHash" text NOT NULL,
                    "Role" character varying(30) NOT NULL,
                    "FullName" character varying(100) NOT NULL,
                    "IsActive" boolean NOT NULL,
                    "CreatedAt" timestamp with time zone NOT NULL DEFAULT CURRENT_TIMESTAMP
                );

                CREATE UNIQUE INDEX IF NOT EXISTS "IX_Users_NormalizedUsername"
                    ON "Users" ("NormalizedUsername");
                """);
        }

        protected override void Down(MigrationBuilder migrationBuilder)
        {
            // Deliberately no-op: a repair migration must never remove login data.
        }
    }
}
